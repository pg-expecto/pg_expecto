# Генерация отчёта по нагрузочному тестированию с использованием DeepSeek

Данный документ содержит пошаговую инструкцию по подготовке аналитического отчёта на основе результатов нагрузочного тестирования СУБД PostgreSQL. Отчёт формируется с привлечением методологии философского анализа и языковой модели DeepSeek.

## Важные условия

- Скрипт `load_test_report.sh` установлен по фиксированному абсолютному пути:
  ```
  /postgres/pg_expecto/sh/performance_reports/load_test_report.sh
  ```
  Изменение пути при инсталляции не предусмотрено.

- Архив `deepseek.zip` формируется на первом шаге и **не загружается** в репозиторий.

- Взаимодействие с DeepSeek производится **вручную** через веб-интерфейс [chat.deepseek.com](https://chat.deepseek.com). Автоматизация не предусмотрена.

## Порядок действий

### 1. Сформировать первичный отчёт по нагрузочному тестированию

Выполните команду:

```bash
/postgres/pg_expecto/sh/performance_reports/load_test_report.sh
```

В результате работы скрипта будет создан архив `deepseek.zip`, содержащий исходные файлы для дальнейшего анализа.

### 2. Подготовить целевую папку и распаковать архив

Создайте отдельную папку (далее — целевая) и распакуйте в неё полученный архив:

```bash
mkdir -p ./deepseek_workspace
unzip deepseek.zip -d ./deepseek_workspace
cd ./deepseek_workspace
```

### 3. Визуально проверить состав файлов

Убедитесь, что в целевой папке присутствуют все пять файлов:

- `_1.settings.txt` — конфигурация СУБД и ОС
- `_2.postgresql_vmstat_iostat.txt` — статистические данные производительности СУБД и ОС
- `_instruction.txt` — инструкция по подготовке отчёта по нагрузочному тестированию
- `_performance_prompt.txt` — промпт для подготовки отчёта по нагрузочному тестированию
- `_philosophical_instruction_prompt.txt` — промпт для подготовки финального отчёта с помощью философской инструкции

Для проверки можно использовать команду `ls -1` или открыть папку в файловом менеджере.

### 4. Загрузить философскую инструкцию

Перейдите по ссылке:  
https://github.com/pg-expecto/Philosophical_instruction/blob/main/Philosophical_instruction_BETA_v5.1.md

Вручную сохраните файл `Philosophical_instruction_BETA_v5.1.md` в целевую папку (например, через «Сохранить как…» в браузере).

### 5. Подготовить технический отчёт `performance.txt` с помощью DeepSeek

1. Откройте новый чат в [DeepSeek](https://chat.deepseek.com).
2. Выберите **«Эксперт режим»**.
3. Включите **«Глубокое мышление»** и **«Умный поиск»**.
4. Загрузите файл `_instruction.txt`.
5. Введите сообщение:
   ```
   Строго следуй инструкции, ожидай входные данные.
   ```
6. Загрузите файлы `_1.settings.txt` и `_2.postgresql_vmstat_iostat.txt`.
7. Введите в качестве следующего сообщения полное содержимое файла `_performance_prompt.txt`.
8. Запустите генерацию ответа.
9. Полученный результат сохраните в текстовый файл `performance.txt` в целевой папке.

### 6. Подготовить финальный аналитический отчёт с философской инструкцией

1. Начните **новый чат** в DeepSeek.
2. Выберите **«Эксперт режим»**.
3. Включите **«Глубокое мышление»** и **«Умный поиск»**.
4. Загрузите два файла:
   - `_instruction.txt`
   - `Philosophical_instruction_BETA_v5.1.md`
5. Введите сообщение:
   ```
   Строго следуй инструкции, ожидай входные данные.
   ```
6. Загрузите файл `performance.txt`.
7. Введите в качестве сообщения полное содержимое файла `_philosophical_instruction_prompt.txt`.
8. Запустите генерацию ответа.
9. Сохраните итоговый результат как финальный аналитический отчёт (например, `final_report.md` или `final_report.txt`).

## Результат

В целевой папке будут находиться:

- `performance.txt` — технический отчёт по нагрузочному тестированию.
- Финальный аналитический отчёт, расширенный философским анализом.

## Примечания

- Все действия, связанные с DeepSeek, выполняются вручную. Скрипты автоматизации не предусмотрены.
- Архив `deepseek.zip` не должен добавляться в систему контроля версий.
- Путь к скрипту `load_test_report.sh` жёстко задан и не требует изменения.
